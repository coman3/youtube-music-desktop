// Disable TsLint for this document... we dont want it looking ugly as while we develop javascript...

function test() {
    console.log("whoot!");
}

/**
 * Execute runtime injection. Use this to bind to events, set global variables etc.
 */
function initInjection() {
    test();
}
